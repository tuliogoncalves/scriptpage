###
VisualStudio Code PHP executablePath in docker
###

1) Create a file 'php.sh' to /usr/local/bin
        sudo copy ./docker/assets/php.sh /usr/local/bin/php

2) Make it executable:
        sudo chmod +x /usr/local/bin/php

